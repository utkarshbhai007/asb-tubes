export const metadata = {
  title: "Factory & Product Gallery",
  description:
    "Tour the ASB Tubes manufacturing facility, production lines, team, and stainless steel pipe and tube products.",
};

export default function GalleryLayout({ children }) {
  return children;
}
